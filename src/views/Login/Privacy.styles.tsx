import { StyleSheet } from 'react-native';
import { Colors } from '@styles';

export default StyleSheet.create({
  scrollViewStyle: {
    flex: 1,
    padding: 16,
  },
  titleText: {
    textAlign: 'center',
  },
  verticalPadding: {
    paddingVertical: 12,
  },
  bottomPadding: {
    paddingBottom: 12,
  },
});
