import { StyleSheet } from 'react-native';

const barHeight = 50;

export default StyleSheet.create({
  barContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    height: barHeight,
    marginHorizontal: 10,
    paddingBottom: 5,
  },
});
