import { StyleSheet } from 'react-native';

export const STICKER_SIZE = 128;

export default StyleSheet.create({
  item: {
    position: 'absolute',
    width: STICKER_SIZE,
    height: STICKER_SIZE,
  },
});
